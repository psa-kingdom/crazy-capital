{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm install && cd apps/web && npx next build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "echo 'install handled in buildCommand'",
  "framework": "nextjs"
}
